meta {
  name: Update Ticket Priority
  type: http
  seq: 1
}

patch {
  url: {{baseUrl}}/tickets/:id/priority
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

params:path {
  id: 1
}

body:json {
  {
    "priority": "high"
  }
}
